<bg=blue;fg=white>              </>
<bg=blue;fg=white> What's next? </>
<bg=blue;fg=white>              </>

  * <fg=blue>Run</> your application:
    1. Change to the project directory
    2. Initialize Storybook in your project: <comment>bin/console storybook:init</> command;
       - This will create basic configuration files and add required dependencies to your package.json.
    3. Run one of the following:
    <comment> npm i && npm run storybook</>
    or
    <comment> yarn && yarn run storybook</>
